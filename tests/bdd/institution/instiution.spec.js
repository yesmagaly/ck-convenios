describe('Institution', function () {
  it('show institution list page', function () {
    // var optionMenu =  element(by.cssContainingText('.navbar-nav .ng-binding', 'Opciones'));
    // var pageTitle = element(by.css('h1'));
    // optionMenu.click();
    // expect(pageTitle.getText()).toContain('Lista de convenios');
  });
});